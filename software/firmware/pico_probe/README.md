# Pico Probe

This directory is for the Debug Probe firmware (based on `debugprobe`).
Typical setup is to flash the standard `debugprobe.uf2` to the Pico 1.

Get it from: https://github.com/raspberrypi/debugprobe/releases
